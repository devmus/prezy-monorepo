// // Verify without redeeming (e.g., show details)
// const v = await verifyGiftcardCode(inputCode);
// if (!v.ok) {
//   if (v.reason === "not_found") { /* show invalid code */ }
//   if (v.reason === "already_redeemed") { /* show redeemed date */ }
// } else {
//   // v.card has the IGiftcardDoc (safe to map with toGiftcardDTO)
// }
